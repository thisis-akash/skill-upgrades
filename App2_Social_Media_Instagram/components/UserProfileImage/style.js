import { StyleSheet } from 'react-native';

export default style = StyleSheet.create({
    userImageContainer: {
        borderColor: '#F35BAC',
        borderWidth: 1,
        padding: 3,
        borderRadius: 50
    }
});